#include "LicenseHooks.h"
#include "Macros.h"
#include "entry.h"

namespace LicenseHooks {

    void Install() {
        // No hooks to install in this build — legacy patterns removed.
    }

    void Uninstall() {
        // Nothing to uninstall.
    }
}
